# Calendar View Specification

> 日曆視圖功能規格書，用於在交易紀錄頁面提供日曆式的瀏覽體驗。

---

## 1. 功能概述 (Overview)

日曆視圖提供一個直覺的日曆介面，讓使用者可以以月/週/日的方式瀏覽交易紀錄。

### 目標

- 提供替代列表的日曆式瀏覽體驗
- 讓使用者快速查看特定日期的消費
- 支援拖放調整交易日期，提升操作效率

### 範圍

| 包含              | 不包含       |
| ----------------- | ------------ |
| 月/週/日視圖      | 定期交易預覽 |
| 格子內交易摘要    | 帳單提醒通知 |
| 點擊展開 Modal    | 跨裝置同步   |
| 拖放調整日期      | -            |
| Tab 切換列表/日曆 | -            |

---

## 2. UI/UX 設計 (User Interface)

### 2.1 視圖切換 (Tab Switch)

交易紀錄頁面提供兩種視圖模式，透過 Tab 切換：

| Tab      | 說明                   |
| -------- | ---------------------- |
| **列表** | 現有的表格式交易列表   |
| **日曆** | 新增的日曆視圖（預設） |

**規則**：

- 兩個視圖**不共用篩選器**，各自獨立操作
- 日曆視圖使用 React Big Calendar 原生的月/週/日切換與導航
- 切換視圖時保留當前月份/週次的 context

### 2.2 日曆格子顯示 (Cell Display)

每個日期格子內顯示該日的交易摘要：

| 項目          | 規則                            |
| ------------- | ------------------------------- |
| **顯示數量**  | 最多 2 筆交易                   |
| **超過 2 筆** | 顯示 `...` 或 `+N more` 提示    |
| **排序**      | 按 `time` 欄位升序（早 → 晚）   |
| **單筆格式**  | `[分類 Icon] [描述摘要] [金額]` |

**範例**：

```
┌────────────────────────────┐
│ 15                         │
│ 🍴 午餐          -$150   │
│ 🏦 轉帳入        +$500   │
│ ... +2 more              │
└────────────────────────────┘
```

> **說明**：Icon 使用分類的 `icon` 欄位，透過 `CategoryIcon` 元件渲染。

**極端情況處理**：

- 使用 `min-height` 確保格子高度一致
- 內容超過時使用 `overflow: hidden` + `text-overflow: ellipsis`
- 手機版可考慮只顯示 1 筆 + `+N more`

### 2.3 點擊互動 (Click Interaction)

| 操作             | 行為                                                       |
| ---------------- | ---------------------------------------------------------- |
| **點擊日期格子** | 開啟 Modal，顯示該日所有交易明細                           |
| **點擊單筆交易** | 開啟該筆交易的編輯 **Sheet**（複用 `newTransactionSheet`） |

**Modal 內容**：

- 標題：日期（如 `2026/02/05 (三)`，使用 `date-fns` 格式化）
- 列表：當日所有交易，按時間排序，**使用 virtual scroll 處理大量交易**
- 每筆顯示：**分類 Icon**、時間、類型、金額、分類名稱、描述
- 底部：當日收入/支出/結餘摘要

> [!NOTE]
> 使用 `@tanstack/react-virtual` 實作 virtual scroll，確保單日交易過多時的滾動效能。

### 2.4 拖放調整日期 (Drag & Drop)

使用者可拖放交易事件到其他日期，快速修改交易日期。

| 項目           | 規則                             |
| -------------- | -------------------------------- |
| **可拖放對象** | 日曆上的交易事件                 |
| **放置目標**   | 其他日期格子                     |
| **更新欄位**   | 僅更新 `date`，不更新 `time`     |
| **操作處理**   | 拖放扣款方時，收款方同步更新日期 |
| **視覺反饋**   | 拖放中顯示半透明預覽             |

**API 呼叫**：拖放完成後呼叫 `PUT /transactions/:id` 更新 `date`。

### 2.5 顏色區分 (Color Coding)

使用現有 `transactionTable.tsx` 的顏色定義（Tailwind Classes）：

| 交易類型           | Text Color | Background       |
| ------------------ | ---------- | ---------------- |
| **收入 (INCOME)**  | 綠色       | `bg-emerald-500` |
| **支出 (EXPENSE)** | 紅色       | `bg-rose-500`    |
| **操作 (OPERATE)** | 橙黃色     | `bg-amber-500`   |

**操作類交易特殊處理**：

- 操作 (OPERATE) 是主類別，轉帳只是其中一個子分類
- 日曆上**只顯示扣款方**（`targetAccountId` 不為 null 且 `type` 為 EXPENSE）
- 收款方不顯示，避免重複
- 樣式與一般支出有明顯區別（使用橙黃色 `amber` + 操作 icon）

---

## 3. 資料結構 (Data Structure)

> 本功能不需要調整 DB Schema，現有 Transaction 欄位已足夠。
> 完整 Schema 請參考 [transaction_spec.md](./transaction_spec.md)。

### 3.1 Transaction 與 CalendarEvent 轉換

前端需將 Transaction 資料轉換為 React Big Calendar 的 Event 格式：

**來源欄位**：

| Transaction 欄位 | 用途              |
| ---------------- | ----------------- |
| `id`             | 事件唯一識別      |
| `date`           | 決定顯示日期      |
| `time`           | 用於 Modal 內排序 |
| `type`           | 決定顏色          |
| `amount`         | 顯示金額          |
| `description`    | 事件標題          |
| `linkId`         | 判斷是否為轉帳    |

**轉換函式**：

```typescript
interface CalendarEvent {
  id: string;
  title: string;
  start: Date;
  end: Date;
  allDay: boolean;
  // 額外資料供樣式處理
  type: RootType;
  amount: number;
  isTransfer: boolean;
}

function transactionToCalendarEvent(tx: TransactionType): CalendarEvent {
  // 強制使用 UTC 解析，確保 Date object 的 timestamp 在不同時區一致
  // 這樣 RBC 內部排序、定位不會因為用戶切換裝置/時區而錯亂
  const dateTime = new Date(`${tx.date}T${tx.time}Z`);
  return {
    id: tx.id,
    title: tx.description || formatAmount(tx.amount, tx.type),
    start: dateTime,
    end: dateTime,
    allDay: false,
    type: tx.type,
    amount: tx.amount,
    isTransfer: tx.linkId !== null,
  };
}
```

> **時區設計說明**：
> DB 儲存的是使用者**當下記錄的當地時間**（`YYYY-MM-DD HH:mm:ss`），不包含時區資訊。
> 前端解析時**強制加上 `Z`（UTC）**，確保 Date object 的 timestamp 一致，
> 避免用戶在不同時區的裝置上看到事件排序錯亂的問題。

### 3.2 操作類交易處理

操作類交易（如轉帳）會產生 2 筆交易（扣款方 + 收款方），日曆上只顯示扣款方。

**篩選方式**：後端 SQL 直接排除，不傳給前端。

**SQL WHERE 條件**：

```sql
-- 日曆查詢：排除操作類的收款方
WHERE NOT (
  "targetAccountId" IS NOT NULL
  AND "type" = '收入'
)
```

> **現有實作**：後端 `getTransactionsByDate` 已有此篩選邏輯（第 69-75 行），不需額外處理。

**拖放操作類交易時的同步**：

> [!IMPORTANT]
> **必做**：後端需實作 `linkId` 連動更新。
>
> 目前 `updateIncomeExpense` **沒有** 處理 `linkId` 連動，只有 `deleteTransaction` 有。
> 拖放扣款方時，後端應在同一個 Database Transaction 內同步更新收款方的 `date`。
>
> 如果前端分兩次 API 呼叫，第一次成功、第二次失敗，資料會不一致。

- 拖放扣款方時，後端自動同步更新收款方的 `date`
- 前端只需發送一次 `PUT /transactions/:id`，後端處理連動

### 3.3 共用函式定義

以下函式需新增至 `packages/shared/src/utils/transactionUtils.ts`，供前後端共用：

```typescript
import { TransactionType, RootType } from '@repo/shared';

/**
 * 判斷是否為操作類交易（有 targetAccountId）
 */
export function isOperateTransaction(tx: TransactionType): boolean {
  return tx.targetAccountId !== null && tx.targetAccountId !== undefined;
}

/**
 * 判斷是否為操作類交易的扣款方（type = EXPENSE 且有 targetAccountId）
 */
export function isOutgoingTransfer(tx: TransactionType): boolean {
  return isOperateTransaction(tx) && tx.type === RootType.EXPENSE;
}

/**
 * 判斷是否為操作類交易的收款方（type = INCOME 且有 targetAccountId）
 */
export function isIncomingTransfer(tx: TransactionType): boolean {
  return isOperateTransaction(tx) && tx.type === RootType.INCOME;
}
```

> **注意**：目前 `transactionTable.tsx` 內有 local 的 `isTransfer` 函式，
> 實作時應重構為使用上述共用函式。

---

## 4. API 設計 (Backend Integration)

### 4.1 現有 API 適用性

日曆視圖使用現有 API，不需新增 endpoint。

| 功能     | 使用 API                | 說明                                          |
| -------- | ----------------------- | --------------------------------------------- |
| 取得交易 | `GET /transactions`     | 依照 `startDate`, `endDate` 參數查詢          |
| 更新日期 | `PUT /transactions/:id` | 拖放後更新 `date` 欄位（後端自動連動 linkId） |
| 查看明細 | `GET /transactions/:id` | Modal 需要時使用                              |

**效能考量**：

- 日曆需要當月所有交易（不分頁），現有 API 預設 `limit = 10`
- **初版做法**：先全部撈回來試試，未來用 Seeder 塞入完整假資料測試效能
- **未來優化**：若有效能問題，可新增 `?noPagination=true` 參數或專用 endpoint

### 4.2 拖放更新 API

**Request**：

```http
PUT /transactions/:id
Content-Type: application/json

{
  "date": "2026-02-10"
}
```

**操作類交易處理**：

> [!IMPORTANT]
> 後端需修改 `updateIncomeExpense`，在 DB Transaction 內同步更新 linked transaction。

```typescript
// transactionServices.ts - updateIncomeExpense 需新增的邏輯
if (transaction.linkId) {
  const linkedTransaction = await Transaction.findOne({
    where: { id: transaction.linkId, userId },
    transaction: t,
  });
  if (linkedTransaction) {
    await linkedTransaction.update({ date: data.date }, { transaction: t });
  }
}
```

---

## 5. 技術實作 (Implementation)

### 5.1 套件選擇

| 套件                        | 用途                    | License |
| --------------------------- | ----------------------- | ------- |
| **react-big-calendar**      | 日曆元件                | MIT     |
| **date-fns**                | 日期處理 + localizer    | MIT     |
| **react-dnd**               | 拖放功能（RBC 內建）    | MIT     |
| **@tanstack/react-virtual** | Modal 內 virtual scroll | MIT     |

**安裝**：

```bash
pnpm add react-big-calendar date-fns @tanstack/react-virtual
pnpm add -D @types/react-big-calendar
```

### 5.2 元件架構

```
src/components/transactions/
├── transactionCalendar.tsx      # 日曆主元件
├── calendarEvent.tsx           # 单筆事件渲染
├── calendarDayModal.tsx        # 點擊日期的 Modal
└── calendarToolbar.tsx         # 自訂工具列（可選）

修改：
src/pages/transactions/index.tsx  # 新增 Tab 切換邏輯
```

---

## 6. 測試方向 (Testing Strategy)

### 6.1 單元測試

| 測試項目                     | 說明                   |
| ---------------------------- | ---------------------- |
| `transactionToCalendarEvent` | 轉換函式正確性         |
| `filterForCalendar`          | 轉帳篩選邏輯           |
| 顏色判斷                     | 不同 type 對應正確顏色 |

### 6.2 E2E 測試

| 測試情境   | 驗證點                          |
| ---------- | ------------------------------- |
| 視圖切換   | Tab 切換後顯示正確視圖          |
| 日曆顯示   | 交易顯示在正確日期              |
| 點擊 Modal | 點擊日期開啟 Modal，內容正確    |
| 拖放更新   | 拖放後交易日期更新且 API 被呼叫 |
| 轉帳顯示   | 只顯示扣款方，且為橙黃色        |

---

## 7. 未來功能 (Future Enhancements)

以下功能不在本次實作範圍，保留作為未來擴充：

| 功能         | 說明                                     | 優先級 |
| ------------ | ---------------------------------------- | ------ |
| 定期交易預覽 | 顯示未來的 recurring transactions        | Medium |
| 記帳熱力圖   | 類似 GitHub contribution graph           | Low    |
| 月度摘要條   | 日曆上方顯示總收入/支出/結餘             | Medium |
| 快速記帳     | 點擊空白日期直接新增交易                 | Medium |
| 篩選器整合   | 日曆視圖支援分類/帳戶篩選                | Low    |
| 效能優化     | `?noPagination=true` 參數或專用 endpoint | Low    |
